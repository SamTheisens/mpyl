name: build
on:
  push:
    branches-ignore: [ 'main' ]

jobs:
  build-pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          cache: 'pipenv'
      - name: Install pipenv
        run: curl https://raw.githubusercontent.com/pypa/pipenv/master/get-pipenv.py | python
      - run: pipenv install

      - name: 'bla'
        run: git fetch -v -- origin +refs/heads/main:refs/heads/main

      - name: Run build
        run: pipenv run run
        env:
          CHANGE_ID: 1234